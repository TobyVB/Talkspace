
export default function RetrievePassword(props){
    
    return (
        <div className="page-body retrieve-password">
            <h1>Retrieve Password</h1>
            <button onClick={props.cancel}>cancel</button>
        </div>
    )
}